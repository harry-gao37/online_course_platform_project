# 服务端口

server:
  port: 10000

# 服务名
spring:
  application:
    name: canal-client
  profiles:
    active: dev



    #数据库连接
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/guli?serverTimezone=GMT%2B8
    username: root
    password: root